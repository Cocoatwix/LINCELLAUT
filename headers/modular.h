
#ifndef MODULAR_H
#define MODULAR_H

/** Returns the inverse of a given number
    under some given modulus. Returns -1 if
		the inverse DNE. */
int num_inverse(int, int);

#endif //MODULAR_H